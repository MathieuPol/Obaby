{% extends 'base.html.twig' %}

{% block title %}Answer{% endblock %}

{% block navbar %}{% include '/Back/partials/_nav_back.html.twig' %}{% endblock %}

{% block body %}
    <h1>Answer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ answer.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ answer.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ answer.createdAt ? answer.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ answer.status }}</td>
            </tr>
        </tbody>
    </table>

{#     <a href="{{ path('app_answer_index') }}">back to list</a> #}

    <a href="{{ path('back_answer_update', {'id': answer.id}) }}">edit</a>

    {{ include('Back/answer/_delete_form.html.twig') }}
{% endblock %}

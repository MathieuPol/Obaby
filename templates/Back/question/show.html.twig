{% extends 'base.html.twig' %}

{% block title %}Question{% endblock %}

{% block body %}
    <h1>Question</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ question.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ question.createdAt ? question.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ question.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cack_question_list') }}">back to list</a>

    <a href="{{ path('back_question_update', {'id': question.id}) }}">edit</a>

    {{ include('Back/question/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_practice_delete', {'id': practice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ practice.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block navbar %}{% include '/Back/partials/_nav_back.html.twig' %}{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Rôles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Pseudonyme</th>
                <td>{{ user.pseudo }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ user.status }}</td>
            </tr>
        </tbody>
    </table>

{#     <a href="{{ path('app_back_user_index') }}">back to list</a> #}

    <a href="{{ path('back_user_update', {'id': user.id}) }}">Editer</a>

    {{ include('back/user/_delete_form.html.twig') }}
{% endblock %}

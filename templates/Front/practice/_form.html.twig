{{ form_start(form) }}

    <div class="text-center">
            <div class="row-1">
                <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                    <p class="text-center"><strong>{{ form_row(form.title) }}</strong></p>
                </div>
            </div>
            <div class="row-1">
                <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                    <p class="text-center"><strong>{{ form_row(form.content) }}</strong></p>
                </div>
            </div>
            <div class="row-3">
                <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                    <p class="text-center"><strong>{{ form_row(form.category) }}</strong></p>
                </div>
            </div>
            <div class="row-1">
                <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                    <div class="dropdown d-flex d-xxl-flex justify-content-center align-items-center justify-content-xxl-center">
                        <a href="{{ path('question_ask') }}" class="animated-link submit-link">
                            <button type="submit" class="btn text-white">{{ button_label|default('Soumettre') }}</button>
                        </a>
                    </div>
                </div>
            </div>    
    </div>
</div>

{{ form_end(form) }}

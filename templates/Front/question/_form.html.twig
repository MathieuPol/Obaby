{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
</div>
    <div class="text-center">
                    <div class="row-1">
                        <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                            <p class="text-center"><strong>{{ form_row(form.content) }}</strong></p>
                        </div>
                    </div>
                    <div class="row-3">
                        <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                            <p class="text-center"><strong>{{ form_row(form.category) }}</strong></p>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                            <p class="text-start">* champs obligatoires</p>
                        </div>
                    </div>
                </div>
            <div class="row">
                <div class="col-10 col-sm-10 col-md-8 offset-1 offset-sm-1 offset-md-2">
                    <div class="dropdown d-flex d-xxl-flex justify-content-center align-items-center justify-content-xxl-center">
                        <a href="#" class="animated-link submit-link">
                            <span class="btn text-white submit-btn">{{ button_label|default('Soumettre') }}</span>
                        </a>
                    </div>
                </div>
            </div>    
        </form>
    </div>
</div>
{{ form_end(form) }}
